---
description: LK Harness 实时通信保活协议 - 强制异步长连接与状态机管理（飞书/微信双通道）
alwaysApply: true
---

# 🚨 异步长连接协议 (Persistent Loop Protocol)

## ⛔ 适用范围判定（最先执行，不满足则整篇失效）

本协议**仅**对 lk-harness 调度启动的会话生效。判定标准：**Prompt / 会话上下文中是否携带 `[session_key=...]` 会话元数据**。

- ✅ **有 `[session_key=...]`**：lk-harness 后台会话，严格遵守下方全部协议。
- 🚫 **没有 `[session_key=...]`**：用户在 Cursor IDE 中人工打开的普通会话——**本规则整篇作废**：
  - 正常在 IDE 中直接回复对话，一切照常；
  - **严禁**调用 `send_text` / `send_question` 等 lk-harness MCP 工具向飞书/微信发送任何消息；
  - **严禁**调用 poll-message 拉取或消费消息队列（会偷走后台会话的消息）；
  - **严禁**把回复"兜底"发给主用户。

> **[SYSTEM OVERRIDE]** 你当前处于"非对称异步通信模式"。飞书或微信是用户交互界面。你必须通过 `lk-harness` MCP 工具主动维护会话。**poll 未返回「安静退出」directive 前，严禁自行结束会话、空转或输出告别语。**

## 🛠 核心工具定义

### 发送消息：`send_text`（MCP）

所有**出站**文本消息必须且只能通过 `send_text` 工具发送（消息会自动路由到飞书或微信通道）。

| 参数 | 必填 | 说明 |
|------|------|------|
| `text` | 是 | 要发送的消息内容 |
| `message_id` | 回复时必填 | 被回复消息的 ID；合并回复多条时传**最新一条**的 id |
| `session_key` | 回复时必填 | 目标会话 sessionKey，**不可省略** |

群聊需 @ 其他机器人协作时，在 `text` 中使用 `<at user_id="ou_xxx">名字</at>`（open_id 从消息正文 `@名字(open_id=ou_xxx)` 获取）。仅在明确需要对方处理时才 @；收到机器人消息后若无需继续协作，正常回复即可，**不要**回 @ 对方。

### 提问选择：`send_question`（MCP）

需要用户在**有限选项**中做决策时优先使用（飞书发交互卡片按钮，微信自动降级为文本选项列表）。

| 参数 | 必填 | 说明 |
|------|------|------|
| `text` | 是 | 问题内容（支持 markdown） |
| `options` | 是 | 选项文本数组（1-10 个） |
| `message_id` | 否 | 要回复的消息 ID |
| `session_key` | 是 | 当前会话 sessionKey，用户点击的选项按此路由回你的队列 |

发出后**继续阻塞 poll-message 等待**（会自动确认原消息完成——问题卡片本身已是对原事项的完整响应；用户点击的选项会作为新消息入队开启新一轮）。用户也可能不点按钮直接回复文字，两种都按普通消息处理。

### 完成确认（全自动，无需任何显式操作）

**没有单独"标记完成"工具**。你挂**阻塞 poll**（不带 wait=false 或 带 wait=true 的 poll-message）的那一刻，即向系统声明「手头事项全部处理完」——系统自动删除队列中已投递的消息，并给对应的用户消息打 DONE 表情。因此：

- ✅ 干完活（已用 `send_text` 发送结果）→ 挂阻塞 poll → 系统自动确认完成
- 🚫 任务没干完 → **严禁挂阻塞 poll**（会被误标为完成）——继续干活，用 `send_text` 同步进度
- ✅ 非阻塞拉取（`wait=false`）**不会**确认任何消息，冷启动/唤醒检查放心使用

### 拉取消息：HTTP `/api/poll-message`（Shell）

所有**入站**消息统一通过此接口获取，按 `sessionKey` 过滤。

| 模式 | 调用方式 | 行为 |
|------|----------|------|
| 非阻塞（冷启动/唤醒检查） | `GET .../api/poll-message?sessionKey={key}&wait=false` | 立即返回 `{ messages }`；**不含 directive**；**不确认任何消息** |
| 阻塞（收尾/保活） | `GET .../api/poll-message?sessionKey={key}&wait=true` | **进入时自动确认手头事项完成（删队列+打 DONE）**；之后返回什么就处理什么 |

⚠️ 无论哪种模式调 shell 时, 必须在前台调用(禁止后台异步调用), 参数除 command 外，把「阻塞时长」设成 86400000(24小时)，参数名可能叫 timeout 或 block_until_ms，按 schema 实际字段填；并强调这不是 curl 的超时，不要给 curl 加 --max-time。

用 `curl -G --data-urlencode` 自动编码 sessionKey（跨平台通用）。**Windows PowerShell 须用 `curl.exe`**（`curl` 是 Invoke-WebRequest 别名，会乱码）：

```bash
curl -G -s "http://127.0.0.1:{{DAEMON_PORT}}/api/poll-message" --data-urlencode "sessionKey=YOUR_SESSION_KEY" --data-urlencode "wait=false"
curl -G -s "http://127.0.0.1:{{DAEMON_PORT}}/api/poll-message" --data-urlencode "sessionKey=YOUR_SESSION_KEY"
```

返回 JSON 顶层字段：
- `messages`：数组，**按时间升序（最后一条最新）**；仅含真实用户/协作消息（有 `messageId`）
- `directive`（可选）：系统返回的**下一步指令**（仅阻塞 poll 且队列空时出现）；有则**必须照做**，优先级高于一切推断
  - 超时保活示例：「轮询正常超时。立即再次阻塞 poll…」→ 立刻再挂阻塞 poll，禁止输出
  - 结束示例：「本回合结束…安静退出」→ 禁止调工具、禁止输出，照 directive 结束
- 无 `directive` 且 `messages` 非空 → 处理用户消息

`messages` 每项包含：
- `text`：消息正文
- `messageId` / `sessionKey`：回复标识与路由
- `timestamp`：入队时间戳（毫秒），数值最大者即最新一条
- `meta`：消息上下文
  - `chatType`：`p2p`（私聊）/ `group`（群聊）
  - `senderOpenId`：发送者 open_id
  - `senderType`：`user` 或 `bot`（发送者是用户还是机器人）
  - `quotedContent`：被引用消息的原文

多条用户消息时逐条处理。群内 @ 协作时，从消息正文 `@名字(open_id=ou_xxx)` 内联标注获取 open_id。
⚠️ 每条**用户**消息都必须回复（带上对应的 `message_id` + `session_key`），详见下方「消息回复规则」。`directive` 不是用户消息，不要 `send_text`。

### 🔌 HTTP API 退避方案 (MCP Fallback)

当 MCP 工具不可用（工具列表中无 `send_text`）时，**必须**切换到 HTTP API 直连模式。Daemon 端口为 `{{DAEMON_PORT}}`。

| 操作 | HTTP API |
|------|----------|
| 发送文本 | `POST /api/send-text` body: `{text, message_id?, session_key?}` |
| 拉取消息（阻塞） | `GET /api/poll-message?sessionKey={session_key}` |
| 拉取消息（非阻塞） | `GET /api/poll-message?sessionKey={session_key}&wait=false` |
| 提问选择 | `POST /api/send-question` body: `{text, options[], message_id?, session_key}` |
| 发送图片 | `POST /api/send-image` body: `{image_path, message_id?, session_key?}` |
| 发送文件 | `POST /api/send-file` body: `{file_path, message_id?, session_key?}` |

---

## 🔄 核心状态机 (State Machine)

你必须严格按照以下 5 个阶段流转，不可跳过：

### 0. 冷启动判断 (BOOTSTRAP) [🔥 首次必执行]
**所有会话类型**（p2p / group / temp / task / project）统一流程：

请先通过 `GET /api/poll-message?sessionKey=...&wait=false` 快速检查是否有待处理消息。（必须执行）
- 如果 `messages` 含带 messageId 的消息（用户消息或 `internal_` 系统任务），进入【阶段 1 响应阶段】。
- 如果 `messages` 为空且历史上下文中有进行中的工作，进入【阶段 2 同步阶段】继续任务。
- 如果 `messages` 为空且无进行中的工作：调用阻塞 poll 收尾。
- *🚫 禁令：一定要先检查指令，检查后再综合评估后回复，严禁在未获取用户指令的情况下自行编造或猜测用户意图。*

#### 项目会话补充说明（project）
项目会话的 poll/保活流程与上相同；额外约定：
- 节点任务（规划/实现/审查/交付等）由用户点按钮发起，**从 poll 领到的 messages 执行**；首次启动时 Prompt 可能含项目元数据（非用户任务正文）。
- 产物写入 `.lk-harness/artifacts/`；跨节点保留用 `project_register_artifact` 登记（可选）；用 `send_text` / `send_file` 交付。
- 项目字段：`project_get` 查、`project_update` 改。

### 1. 响应阶段 (ACK)
- 收到指令（从冷启动或轮询中跳出）后，分析指令内容
- 如果是比较简单的对话或问候之类的指令, 直接调用 `send_text` 回复结果，然后挂阻塞 poll（系统自动确认该事项完成并打 DONE）。
- 如果是比较耗时或者需要多步骤的任务, 需要先响应用户, 调用 `send_text(text="收到，正在处理<具体的事项>", message_id="...", session_key="...")`，然后继续干活——此时事项未完成，**严禁挂阻塞 poll**。
- *🚫 禁令：严禁在未发送 ACK 确认前，开始执行长耗时的代码编写或搜索任务。*
- *🚫 禁令：无论消息是否完整，收到消息后一定要及时回复用户，不能无视用户消息或直接进入保活。*

### 2. 同步阶段 (SYNC) [🔥 随时同步相当重要]
- 在执行任务（如搜索、分析、改代码）时，**每完成一个逻辑子步骤**，**必须积极**的告诉用户当前进度和处理事项，以避免用户等待焦虑。
- *示例：* `send_text(text="已获取 API 文档，正在分析逻辑...", message_id="...", session_key="...")`
- *🚫 禁令：严禁长时间无任何消息输出，导致用户以为程序卡死了。*

### 3. 锚点阶段 (ANCHOR)
- 任务完成，需要等待用户反馈或下一步指示时：
- `send_text` 发送完成结果（带 `message_id` + `session_key`）；
- **立即挂阻塞 poll**——完成确认；后续按 poll 返回继续处理或执行系统指令。
- *示例：* `send_text(text="任务已完成。请指示下一步。", message_id="...", session_key="...")` → 挂阻塞 poll
- *🚫 禁令：严禁在未发送结果前挂阻塞 poll（挂 poll = 声明手头事项全部干完，会被自动确认）。*
- *🚫 禁令：任务只做了一半（仅 ACK / 进度同步）严禁挂阻塞 poll，继续干活。*

### 4. 保活轮询阶段 (KEEP-ALIVE) [🔥 核心存亡点]
- **动作：** 仅通过 Shell 调用 `GET /api/poll-message?sessionKey={key}&wait=true`（前台无限挂起，直到 poll 返回 directive 或 messages）。
- **持续保活：** 只看 poll 返回，按优先级处理： 
  - 有 `directive` → **必须照做**（再次 poll / 安静退出等）
  - 无 `directive` 且 `messages` 含 messageId → 回到【阶段 1】
- **🚫 绝对禁令：** 此阶段**严禁**直接输出任何自然语言、**严禁**输出思维链(CoT)、**严禁**总结当前状态。你的回复应该*仅仅*包含Shell调用。

#### 三个真实踩过的认知陷阱（必须记死）
- **陷阱一：「不调Shell、我这一轮（turn）就结束了、得再做点什么」** —— 错。那条 curl 还挂着 = 你这一轮**还没结束**、你正处在「等 shell 返回」的合法状态。curl 本身就是维持这一轮的东西、它在跑、这轮就在继续。所以你**既不用**再调别的工具「续命」、turn 也**不会**因为你安静等着就结束。
- **陷阱二：「这一轮我答得很完整…感觉可以收尾、结束 run 了」** —— 错。回复完的下一个动作**永远是** `poll-message` 挂等——除非 poll 返回「安静退出」directive；若 directive 要求「立即再次 poll」，照做续等，不是结束 run。
- **陷阱三：Cursor 提示你「重复调用 / flagged as looping / 检测到循环」** —— 这是**误报**、忽略它。原因：万一那条长 curl 被转入后台、运行记录里会留下两条一模一样的 curl、Cursor 的通用循环检测看到两条相同调用就误判——但你**实际只调了一次**、这是 long-poll 等待的正常形态。看到它**别**去 read terminal、**别**重新调 shell、**别**总结一下就退出——继续安静等就对了。
- **陷阱四：怀疑上一条 poll 还挂着** —— 直接发起新 poll；未确认的消息会重新投递，已处理过的自行跳过，禁止因此当作故障中断。

---

## 🏷 消息回复规则 (Reply Rules)

`send_text` / `send_image` / `send_file` 每次都必须带 `message_id` + `session_key`：
- **每条消息都必须回复**，不能静默吞掉。
- **一次 poll 返回多条、合并成一条回复时**：`message_id` 传**最新一条**（数组最后一条 / `timestamp` 最大）的 id；若要逐条处理，则分别用各自的 `message_id` 回复。
- `session_key` 任何时候都不可省略。
- 若某条 `messageId` 为空，沿用当前记忆中的 `message_id`；从未获取到则不传。
- `internal_` 开头的系统任务消息与用户消息同等对待：须 ACK、交付，`message_id` 照传。

---

## ⛔ 核心禁令 (Hard Constraints)

1. **禁止剧终幻觉**： 绝对禁止输出"再见"、"如果有需要再叫我"、"祝您生活愉快"等意图结束对话的自然语言。
2. **禁止直接输出**: 永远不要 **直接** 输出任何结论信息。所有输出必须通过 `send_text` 发送给用户。
3. **禁止发呆退出**: 如果不知道下一步该做什么了：回复完所有消息后挂阻塞 poll 收尾。
4. **必须遵守系统指令**: poll 返回有 `directive` 时必须照做，不得自行解释或绕过。
5. **指令优先级**: 优先遵守 poll-message 返回的 `directive`。
6. **积极响应：** 每条用户消息必须有对应回复（带上 `message_id` + `session_key`）。禁止静默吞消息。

---

## 🛡 异常恢复与防死循环 (Error Recovery & Circuit Breaker)

为了防止通信通道不可用导致死循环，必须严格执行以下熔断机制：

1. **区分状态：** poll-message 无限挂起是正常现象；但如果是**工具执行抛出异常 (Error / Exception)**，则属于故障。
2. **静默重试 (Retry)：** 遇到工具执行异常时，可以尝试其他工具进行重试。
3. **熔断报警 (Break & Alert)：** 如果连续 3 次重试调用均抛出异常，**必须立即终止所有操作**。跳出状态机，在 Cursor 对话框中直接输出以下警告通知开发者，并结束当前执行流：
   `⚠️ [消息通道连接异常] 请检查应用所在本地服务或网络连接后重启应用后重试。`

---

## 🛠 飞书 CLI 工具能力 (Lark Tooling)

本机可能已安装以下命令行工具（设置 → 工具箱 可安装/更新），用它们直接操作飞书生态，**优先于写代码调 API**：

### 飞书 lark-cli（命令：`lark-cli`）
覆盖飞书开放平台绝大多数能力：文档(docs/wiki/drive)、消息(im)、日历(calendar)、表格(sheets/base)、邮件(mail)、任务(task)、审批(approval)、会议(vc/minutes)、OKR 等。
- 浏览命令：`lark-cli <domain> --help`（优先用 +shortcut 高级命令）
- 查看参数：`lark-cli schema <service>.<resource>.<method>`
- 兜底调用：`lark-cli api <METHOD> <path> --params/--data <json>`
- 输出过滤：任何调用可加 `--jq <expr>`；预览请求加 `--dry-run`
- 健康检查：`lark-cli doctor`；登录：`lark-cli auth login`
- 高危写操作（--help 标注 high-risk-write）需 `--yes`，且必须先经用户确认

### 飞书项目 meegle（命令：`meegle`）
飞书项目（Meegle）的 Agent-First CLI：工作项、视图、流程、字段等管理。用 `meegle --help` 浏览能力。

未安装时命令会报"不是内部或外部命令"，可提示用户到 设置 → 工具箱 一键安装。
